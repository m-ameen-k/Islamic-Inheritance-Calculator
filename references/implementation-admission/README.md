# Implementation admission records

Store one append-only calculation-readiness admission record per rule here.
Admission records authorize only the named rule and do not load it into a
runtime registry. Production loading requires a separate manual manifest entry.

Stage 4A contains no admission records.
