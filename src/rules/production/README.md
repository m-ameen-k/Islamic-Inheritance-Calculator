# Production rules

This directory is reserved for one admitted rule per TypeScript file. A file is
not loadable merely because it exists here: only the manual production manifest
may authorize it for the generated registry.

Stage 4A contains no production rule implementations.
