import type { FiqhRuleRecord } from "../../domain/rule-source";

/**
 * Production verified rule data is intentionally empty until reviewed rules
 * are extracted from the user's supplied kitabs.
 */
export const VERIFIED_SHAFII_RULES: readonly FiqhRuleRecord[] = Object.freeze([]);
